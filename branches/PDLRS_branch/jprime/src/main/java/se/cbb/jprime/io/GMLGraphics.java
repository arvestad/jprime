package se.cbb.jprime.io;

import java.util.List;

/**
 * Represents a GML Graphics object. Right now this is only a stub for when
 * the actual need of the class arises (see e.g. GMLGraph or GMLEdge for similar implementations).
 * 
 * @author Joel Sjöstrand.
 */
public class GMLGraphics {

	
	/**
	 * Constructor.
	 * @param values all attributes (common and non-common) at the level beneath the Graphics element.
	 * @throws GMLIOException.
	 */
	public GMLGraphics(List<GMLKeyValuePair> values) throws GMLIOException {
		// TODO: Implement!
	}
}
